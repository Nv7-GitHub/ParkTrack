import SwiftUI

struct ParksListScreen: View {
    var body: some View {
        NavigationStack {
            Text("ParksListScreen")
                .navigationTitle("ParksListScreen")
        }
    }
}
