<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  A partial Info.plist, merged into the one Xcode generates.

	  Everything else the app declares is a `INFOPLIST_KEY_*` build setting in project.yml,
	  which is tidier and keeps the values next to the rest of the target. This one key
	  cannot be: `INFOPLIST_KEY_UIBackgroundModes` is accepted by the build system, written
	  into the project, and then ignored by the generator — it is not among the keys that
	  generator understands. The setting was there and the key was not in the built plist,
	  with nothing anywhere to say so.

	  Without it CloudKit mirroring logs "BUG IN CLIENT OF CLOUDKIT: CloudKit push
	  notifications require the 'remote-notification' background mode in your info plist" and
	  falls back to syncing only while the app is in the foreground.

	  GENERATE_INFOPLIST_FILE stays on: Xcode treats this as the base and merges the generated
	  keys on top, so the usage descriptions and the rest still come from project.yml.
	-->
	<!--
	  Answers the export compliance question at upload time rather than in a dialog.
	  Every build otherwise asks whether the app uses non-exempt encryption, on every
	  upload, forever. It does not: CloudKit and HTTPS are exempt under the standard
	  exemption for encryption that is only used to protect a connection, and nothing here
	  implements or ships any of its own.

	  If that ever stops being true — a custom cipher, encrypted local storage of its own —
	  this has to be revisited, because it is a declaration to a regulator and not a build
	  setting.
	-->
	<!--
	  What the icon says on the home screen, which is not what the App Store calls it.
	  Store names are unique across every developer and "ParkTrack" is already taken, so the
	  listing has to carry a longer one. This is the short name, and it has no uniqueness
	  requirement at all — two apps may both call themselves ParkMax on a phone.

	  The target, the module and the bundle id are all still ParkTrack, deliberately. None of
	  them is visible to anybody using the app, and changing the bundle id would mean a new
	  iCloud container, a re-upload of every photo, and abandoning everything already
	  published — a great deal of work to tidy a string only the developer ever reads.
	-->
	<key>CFBundleDisplayName</key>
	<string>ParkMax</string>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
	</array>
</dict>
</plist>
